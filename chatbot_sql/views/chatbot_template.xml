<odoo>
    <template id="chatbot_page" inherit_id="website.layout">
        <xpath expr="//main" position="inside">
            <!-- Main Chatbot Container -->
            <div id="chatbot">
                <!-- Header -->
                <div class="header">
                    <span>Website Assistant</span>
                    <div class="header-buttons">
                        <button id="clearHistory" title="Clear chat history" aria-label="Clear chat history">🗑️</button>
                        <button id="toggleChat" aria-label="Minimize chat">−</button>
                    </div>
                </div>

                <!-- Messages Container -->
                <div id="messagesContainer">
                    <div id="welcomeMessage">
                        Welcome! I can help you find products in our jewelry store. Try asking me about gold rings, silver bracelets, or products under a certain price.
                    </div>
                </div>

                <!-- Input Area -->
                <div class="input-area">
                    <div class="input-container">
                        <input id="chatInput" type="text" placeholder="Ask about your data..." aria-label="Type your message" />
                        <button id="sendBtn" aria-label="Send message">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
                            </svg>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Floating Toggle Button -->
            <div id="chatToggle" aria-label="Open chat assistant" role="button" tabindex="0">
                💬
            </div>
        </xpath>
    </template>
</odoo>
